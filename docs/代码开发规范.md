# AI News Tracker 代码开发规范

本文档定义了 AI News Tracker 项目的前端和后端代码开发规范，旨在确保代码质量、可维护性和团队协作效率。

---

## 📋 目录

- [前端开发规范](#前端开发规范)
- [后端开发规范](#后端开发规范)
- [通用规范](#通用规范)

---

## 🎨 前端开发规范

### 技术栈

- **框架**: React 18.2+
- **语言**: TypeScript 5.2+
- **构建工具**: Vite 5.0+
- **UI 库**: Ant Design 5.12+
- **状态管理**: React Query (TanStack Query) 5.12+
- **路由**: React Router 6.20+
- **HTTP 客户端**: Axios 1.6+
- **日期处理**: Day.js 1.11+
- **Markdown**: react-markdown 10.1+

### 项目结构

```
frontend/src/
├── components/          # React 组件
│   ├── settings/       # 设置相关组件
│   └── ...
├── contexts/           # React Context (状态管理)
├── hooks/              # 自定义 Hooks
├── pages/              # 页面组件
├── services/           # API 服务层
├── types/              # TypeScript 类型定义
└── utils/              # 工具函数
```

### 命名规范

#### 文件命名

- **组件文件**: 使用 PascalCase，如 `ArticleCard.tsx`, `DailySummary.tsx`
- **工具文件**: 使用 camelCase，如 `errorHandler.ts`, `date.ts`
- **类型文件**: 使用 camelCase，如 `index.ts`
- **Hook 文件**: 使用 camelCase，以 `use` 开头，如 `useArticles.ts`, `useErrorHandler.ts`

#### 变量和函数命名

- **变量**: camelCase，如 `articleList`, `isLoading`
- **常量**: UPPER_SNAKE_CASE，如 `STALE_TIME_5M`, `API_BASE_URL`
- **函数**: camelCase，如 `getSummaryText()`, `formatDate()`
- **组件**: PascalCase，如 `ArticleCard`, `DailySummary`
- **类型/接口**: PascalCase，如 `Article`, `ArticleFilter`
- **类型别名**: PascalCase，如 `ImportanceLevel`, `SummaryType`

#### 组件命名

```typescript
// ✅ 正确
export default function ArticleCard({ article }: ArticleCardProps) { }

// ❌ 错误
export default function articleCard({ article }) { }
```

### TypeScript 规范

#### 类型定义

1. **优先使用类型别名而非字面量类型**

```typescript
// ✅ 正确
export type ImportanceLevel = 'high' | 'medium' | 'low';
export interface Article {
  importance?: ImportanceLevel;
}

// ❌ 错误
export interface Article {
  importance?: 'high' | 'medium' | 'low';
}
```

2. **接口定义规范**

```typescript
// ✅ 正确 - 使用 interface 定义对象类型
export interface Article {
  id: number;
  title: string;
  importance?: ImportanceLevel;
}

// ✅ 正确 - 使用 type 定义联合类型或工具类型
export type RSSSourceCreate = Omit<RSSSource, 'id' | 'created_at'>;
```

3. **类型导入规范**

```typescript
// ✅ 正确 - 使用 type 关键字导入类型
import type { Article, ArticleFilter } from '@/types';

// ✅ 正确 - 混合导入
import { apiService } from '@/services/api';
import type { Article } from '@/types';
```

#### 严格模式配置

```json
{
  "compilerOptions": {
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  }
}
```

### 组件开发规范

#### 函数组件

```typescript
// ✅ 正确
import { useState, useEffect } from 'react';
import type { Article } from '@/types';

interface ArticleCardProps {
  article: Article;
}

export default function ArticleCard({ article }: ArticleCardProps) {
  const [expanded, setExpanded] = useState(false);
  
  useEffect(() => {
    // 副作用逻辑
  }, []);
  
  return (
    // JSX
  );
}
```

#### Props 类型定义

```typescript
// ✅ 正确 - 使用 interface 定义 Props
interface ArticleCardProps {
  article: Article;
  onUpdate?: (id: number) => void;
}

// ✅ 正确 - 可选属性使用 ?
interface ModalProps {
  open: boolean;
  title?: string;
  onClose: () => void;
}
```

#### Hooks 使用规范

1. **自定义 Hooks 命名**

```typescript
// ✅ 正确 - 以 use 开头
export function useArticles(filter: ArticleFilter = {}) {
  return useQuery({
    queryKey: ['articles', filter],
    queryFn: () => apiService.getArticles(filter),
  });
}
```

2. **Hooks 依赖管理**

```typescript
// ✅ 正确 - 使用 useCallback 优化回调
const handleClick = useCallback((id: number) => {
  // 处理逻辑
}, [dependency]);

// ✅ 正确 - useEffect 依赖数组完整
useEffect(() => {
  // 副作用逻辑
}, [dependency1, dependency2]);
```

### 状态管理规范

#### React Query 使用

```typescript
// ✅ 正确 - 提取常量
const STALE_TIME_5M = 5 * 60 * 1000;

export function useArticle(id: number) {
  return useQuery({
    queryKey: ['article', id],
    queryFn: () => apiService.getArticle(id),
    enabled: !!id && id > 0,
    staleTime: STALE_TIME_5M,
  });
}

// ✅ 正确 - Mutation 错误处理
export function useDeleteArticle() {
  const queryClient = useQueryClient();
  const { showError, showSuccess } = useErrorHandler();

  return useMutation({
    mutationFn: (id: number) => apiService.deleteArticle(id),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['articles'] });
      showSuccess('文章已删除');
    },
    onError: (error) => {
      showError(error, '删除失败');
    },
  });
}
```

### 错误处理规范

#### 统一错误处理

```typescript
// ✅ 正确 - 使用 useErrorHandler
import { useErrorHandler } from '@/hooks/useErrorHandler';

export function MyComponent() {
  const { createErrorHandler, showSuccess } = useErrorHandler();
  
  const mutation = useMutation({
    mutationFn: apiService.someAction,
    onSuccess: () => {
      showSuccess('操作成功');
    },
    onError: createErrorHandler({
      operationName: '操作名称',
      customMessages: {
        auth: '需要登录',
        server: '服务器错误',
      },
    }),
  });
}
```

### API 服务规范

#### 服务类结构

```typescript
// ✅ 正确 - 使用类封装 API 调用
class ApiService {
  private client: AxiosInstance;
  private token: string | null = null;

  constructor() {
    this.client = axios.create({
      baseURL: API_BASE_URL,
      headers: {
        'Content-Type': 'application/json',
      },
    });
  }

  setToken(token: string | null): void {
    this.token = token;
  }

  async getArticles(filter: ArticleFilter = {}): Promise<ArticleListResponse> {
    return this.handleRequest(
      this.client.get<ArticleListResponse>(`/articles?${params.toString()}`)
    );
  }
}

export const apiService = new ApiService();
```

### 代码组织规范

#### 导入顺序

```typescript
// 1. React 相关
import { useState, useEffect } from 'react';

// 2. 第三方库
import { Card, Button, Space } from 'antd';
import { useQuery, useMutation } from '@tanstack/react-query';

// 3. 项目内部模块
import { apiService } from '@/services/api';
import { useErrorHandler } from '@/hooks/useErrorHandler';
import type { Article } from '@/types';
```

#### 常量提取

```typescript
// ✅ 正确 - 提取魔法数字
const STALE_TIME_30S = 30 * 1000;
const STALE_TIME_5M = 5 * 60 * 1000;
const CONNECT_DELAY = 100;
```

### 样式规范

#### 内联样式

```typescript
// ✅ 正确 - 使用主题工具函数
import { getThemeColor } from '@/utils/theme';

const style = {
  backgroundColor: getThemeColor(theme, 'bgContainer'),
  color: getThemeColor(theme, 'text'),
};
```

#### Ant Design 组件使用

```typescript
// ✅ 正确 - 移除无效的 tip 属性
<Spin size="large" />

// ❌ 错误 - tip 只在嵌套模式下有效
<Spin tip="加载中..." />
```

### 注释规范

```typescript
/**
 * 文章卡片组件
 * 显示文章的基本信息和操作按钮
 */
export default function ArticleCard({ article }: ArticleCardProps) {
  // 状态管理
  const [expanded, setExpanded] = useState(false);
  
  // 业务逻辑
  const handleExpand = () => {
    setExpanded(!expanded);
  };
}
```

---

## 🐍 后端开发规范

### 技术栈

- **框架**: FastAPI 0.104+
- **语言**: Python 3.8+
- **ORM**: SQLAlchemy 2.0+
- **数据验证**: Pydantic 2.0+
- **数据库**: SQLite (sqlite-vec)
- **任务调度**: APScheduler 3.10+
- **HTTP 客户端**: httpx 0.25+, requests 2.31+

### 项目结构

```
backend/app/
├── api/                # API 路由
│   └── v1/
│       ├── api.py      # 路由聚合
│       └── endpoints/  # 端点实现
├── core/               # 核心配置
│   ├── config.py       # 应用配置
│   ├── settings.py     # 设置管理
│   └── security.py     # 安全相关
├── db/                 # 数据库相关
│   ├── models.py       # 数据模型
│   ├── repositories.py # 数据访问层
│   └── session.py      # 数据库会话
├── schemas/            # Pydantic 模式
├── services/           # 业务逻辑层
│   ├── analyzer/       # AI 分析服务
│   ├── collector/      # 数据采集服务
│   ├── notification/   # 通知服务
│   └── ...
├── utils/              # 工具函数
└── main.py             # 应用入口
```

### 命名规范

#### 文件命名

- **Python 文件**: 使用 snake_case，如 `article_service.py`, `ai_analyzer.py`
- **模块文件**: 使用 snake_case，如 `models.py`, `repositories.py`

#### 变量和函数命名

- **变量**: snake_case，如 `article_list`, `is_loading`
- **常量**: UPPER_SNAKE_CASE，如 `API_BASE_URL`, `MAX_RETRIES`
- **函数**: snake_case，如 `get_articles()`, `format_date()`
- **类**: PascalCase，如 `ArticleService`, `AIAnalyzer`
- **私有函数/变量**: 以下划线开头，如 `_validate_config()`, `_internal_state`

#### 类命名

```python
# ✅ 正确
class ArticleService:
    """文章服务类"""
    pass

class AIAnalyzer:
    """AI 分析器"""
    pass
```

### Python 代码规范

#### 类型注解

```python
# ✅ 正确 - 使用类型注解
from typing import Optional, List, Dict

def get_articles(
    filter: Optional[Dict[str, Any]] = None
) -> List[Article]:
    """获取文章列表
    
    Args:
        filter: 过滤条件
        
    Returns:
        文章列表
    """
    pass
```

#### 文档字符串

```python
# ✅ 正确 - 使用 Google 风格文档字符串
def create_ai_analyzer(api_key: Optional[str] = None) -> Optional[AIAnalyzer]:
    """创建AI分析器实例

    Args:
        api_key: OpenAI API密钥（可选，默认从配置读取）

    Returns:
        AI分析器实例，如果未配置API密钥则返回None
    """
    pass
```

#### 导入规范

```python
# 1. 标准库
import sys
from pathlib import Path
from typing import Optional, Dict

# 2. 第三方库
from fastapi import FastAPI, Request
from sqlalchemy.orm import Session

# 3. 项目内部模块
from backend.app.core.config import settings
from backend.app.db import get_db
from backend.app.services.collector import CollectionService
```

### API 端点规范

#### 路由定义

```python
# ✅ 正确 - 使用 APIRouter
from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session

router = APIRouter()

@router.get("/articles", response_model=ArticleListResponse)
async def get_articles(
    filter: ArticleFilter = Depends(),
    db: Session = Depends(get_database)
) -> ArticleListResponse:
    """获取文章列表
    
    Args:
        filter: 过滤条件
        db: 数据库会话
        
    Returns:
        文章列表响应
    """
    pass
```

#### 依赖注入

```python
# ✅ 正确 - 使用依赖注入
from fastapi import Depends
from typing import Generator

def get_database() -> Generator[Session, None, None]:
    """获取数据库会话
    
    Yields:
        Session: SQLAlchemy 数据库会话
    """
    db = get_db()
    with db.get_session() as session:
        yield session

@router.get("/articles")
async def get_articles(db: Session = Depends(get_database)):
    pass
```

### 数据模型规范

#### SQLAlchemy 模型

```python
# ✅ 正确
from sqlalchemy import Column, Integer, String, DateTime
from sqlalchemy.ext.declarative import declarative_base

Base = declarative_base()

class Article(Base):
    """文章表"""
    __tablename__ = "articles"
    
    id = Column(Integer, primary_key=True, autoincrement=True)
    title = Column(String(500), nullable=False, index=True)
    url = Column(String(1000), unique=True, nullable=False, index=True)
    created_at = Column(DateTime, default=datetime.now, nullable=False)
```

#### Pydantic 模式

```python
# ✅ 正确
from pydantic import BaseModel, Field
from typing import Optional

class ArticleBase(BaseModel):
    """文章基础模式"""
    title: str = Field(..., description="文章标题", max_length=500)
    url: str = Field(..., description="文章URL", max_length=1000)

class ArticleCreate(ArticleBase):
    """创建文章模式"""
    pass

class Article(ArticleBase):
    """文章响应模式"""
    id: int
    created_at: datetime
    
    class Config:
        from_attributes = True
```

### 服务层规范

#### 服务类结构

```python
# ✅ 正确
import logging
from typing import List, Optional

logger = logging.getLogger(__name__)

class ArticleService:
    """文章服务类"""
    
    def __init__(self, db: Session):
        self.db = db
    
    def get_articles(
        self, 
        filter: Optional[Dict] = None
    ) -> List[Article]:
        """获取文章列表
        
        Args:
            filter: 过滤条件
            
        Returns:
            文章列表
        """
        try:
            # 业务逻辑
            pass
        except Exception as e:
            logger.error(f"获取文章列表失败: {e}", exc_info=True)
            raise
```

### 错误处理规范

#### 异常处理

```python
# ✅ 正确 - 记录详细错误信息
import logging

logger = logging.getLogger(__name__)

try:
    result = some_operation()
except ValueError as e:
    logger.error(f"值错误: {e}", exc_info=True)
    raise
except Exception as e:
    logger.error(f"未知错误: {e}", exc_info=True)
    raise
```

#### FastAPI 异常处理

```python
# ✅ 正确
from fastapi import Request
from fastapi.exceptions import RequestValidationError
from fastapi.responses import JSONResponse

@app.exception_handler(RequestValidationError)
async def validation_exception_handler(
    request: Request, 
    exc: RequestValidationError
) -> JSONResponse:
    """捕获请求验证错误并记录详细信息"""
    logger.error(f"请求验证失败: URL={request.url}")
    logger.error(f"验证错误详情: {exc.errors()}")
    
    return JSONResponse(
        status_code=422,
        content={"detail": exc.errors()}
    )
```

### 日志规范

#### 日志使用

```python
# ✅ 正确 - 使用统一的日志设置
from backend.app.utils import setup_logger

logger = setup_logger(__name__)

logger.info("操作成功")
logger.warning("警告信息")
logger.error("错误信息", exc_info=True)
```

#### 日志级别

- **DEBUG**: 详细的调试信息
- **INFO**: 一般信息，如操作成功
- **WARNING**: 警告信息，如配置缺失
- **ERROR**: 错误信息，需要记录异常堆栈

### 配置管理规范

#### 环境变量

```python
# ✅ 正确 - 使用 python-dotenv
from dotenv import load_dotenv
import os

load_dotenv()

DATABASE_URL = os.getenv("DATABASE_URL", "sqlite:///default.db")
```

#### 配置类

```python
# ✅ 正确
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    """应用配置类"""
    
    PROJECT_NAME: str = "AI News Tracker"
    VERSION: str = "2.0.0"
    DATABASE_URL: str = "sqlite:///ai_news.db"
    
    class Config:
        env_file = ".env"

settings = Settings()
```

### 代码组织规范

#### 函数拆分

```python
# ✅ 正确 - 单一职责原则
def _validate_provider_config(
    config: Optional[Dict], 
    provider_type: str
) -> bool:
    """验证提供商配置是否有效"""
    if not config:
        logger.warning(f"未选择{provider_type}提供商")
        return False
    return True

def create_ai_analyzer(
    api_key: Optional[str] = None
) -> Optional[AIAnalyzer]:
    """创建AI分析器实例"""
    if not _validate_provider_config(config, "LLM"):
        return None
    # 创建逻辑
    pass
```

---

## 🔄 通用规范

### Git 提交规范

```
<type>(<scope>): <subject>

<body>

<footer>
```

**类型 (type)**:
- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建/工具相关

**示例**:
```
feat(frontend): 添加文章收藏功能
fix(backend): 修复数据库连接池问题
refactor(api): 重构错误处理逻辑
```

### 代码审查检查清单

#### 前端

- [ ] TypeScript 类型定义完整
- [ ] 组件 Props 类型明确
- [ ] 错误处理统一使用 `useErrorHandler`
- [ ] React Query 查询键命名规范
- [ ] 常量提取，无魔法数字
- [ ] 导入顺序规范
- [ ] 组件单一职责
- [ ] 无控制台警告

#### 后端

- [ ] 类型注解完整
- [ ] 文档字符串规范
- [ ] 异常处理完善
- [ ] 日志记录适当
- [ ] 数据库查询优化
- [ ] 函数单一职责
- [ ] 配置管理规范

### 性能优化建议

#### 前端

1. **代码分割**: 使用动态导入进行路由级别的代码分割
2. **React Query**: 合理设置 `staleTime` 和 `cacheTime`
3. **组件优化**: 使用 `React.memo` 和 `useMemo` 优化渲染
4. **图片优化**: 使用适当的图片格式和尺寸

#### 后端

1. **数据库索引**: 为常用查询字段添加索引
2. **查询优化**: 避免 N+1 查询问题
3. **缓存策略**: 对频繁访问的数据使用缓存
4. **异步处理**: 使用异步操作处理耗时任务

### 安全规范

#### 前端

- 敏感信息不存储在 localStorage
- API 密钥不暴露在前端代码
- 用户输入进行验证和转义
- 使用 HTTPS 传输数据

#### 后端

- 输入验证使用 Pydantic
- SQL 注入防护（使用 ORM）
- 认证和授权检查
- 敏感信息加密存储
- 日志不记录敏感信息

---

## 📚 参考资源

- [TypeScript 官方文档](https://www.typescriptlang.org/docs/)
- [React 官方文档](https://react.dev/)
- [FastAPI 官方文档](https://fastapi.tiangolo.com/)
- [Python PEP 8](https://pep8.org/)
- [Ant Design 文档](https://ant.design/)

---

**最后更新**: 2024年
